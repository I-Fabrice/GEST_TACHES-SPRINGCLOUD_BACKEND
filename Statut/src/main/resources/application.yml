server:
  port: 8085

spring:
  application:
    name: statut
  datasource:
    url: jdbc:mysql://localhost:3306/statut?serverTimezone=UTC
    username: 'root'
    password: ''
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.MySQL5Dialect