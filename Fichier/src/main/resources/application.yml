server:
  port: 8084

spring:
  application:
    name: fichier
  datasource:
    url: jdbc:mysql://localhost:3306/fichier?serverTimezone=UTC
    username: 'root'
    password: ''
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.MySQL5Dialect